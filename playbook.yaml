- name: "Test Playbook"
  hosts: test
  tasks:
    - name: Ping hosts
      ansible.builtin.ping:
    
    - name: Print message
      ansible.builtin.debug:
        msg:
          - "Group: {{ groups }}"